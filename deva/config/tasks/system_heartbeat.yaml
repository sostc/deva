config:
  cron_expr: ''
  event_condition: ''
  event_condition_type: contains
  event_source: log
  execution_mode: scheduler
  interval_seconds: 300.0
  run_at: ''
  scheduler_trigger: interval
  task_type: scheduler
func_code: "\nimport logging\nlog = logging.getLogger(__name__)\n\ndef execute() -> dict:\n    \"\"\"心跳：更新系统活跃时间\"\"\"\n \
  \   try:\n        state_mgr = SR('system_state_manager')\n        state_mgr.record_active()\n        log.info(\"[Heartbeat]\
  \ 系统活跃时间已更新\")\n        return {\"success\": True}\n    except Exception as e:\n        log.warning(f\"[Heartbeat] 更新活跃时间失败:\
  \ {e}\")\n        return {\"success\": False, \"error\": str(e)}\n"
metadata:
  created_at: 1776844427.8678
  description: 系统心跳（定期更新活跃时间）
  id: 3ce4ec3e2e65
  name: system_heartbeat
  tags:
  - system
  - heartbeat
  updated_at: 1776844427.8678
