# OS 层 - 注意力操作系统核心

## 用途
注意力操作系统的主控制器层，负责整体调度和协调。

## 待迁移文件
```
attention_os.py      # 主控制器（当前在根目录）
attention_fusion.py # 注意力融合
qkv.py              # QKV 计算
```

## 架构位置
```
┌─────────────────────────────────┐
│          OS 应用层               │
│  (MarketScheduler, Allocator)   │
└─────────────────────────────────┘
                  ↓
┌─────────────────────────────────┐
│        Attention Kernel          │
└─────────────────────────────────┘
```
