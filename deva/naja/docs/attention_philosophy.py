"""

 Attention Kernel 哲学释义

 境随心转，执处成真

 ─────────────────────────────────────

 境随心转（External circumstances change according to the mind）

    在 Attention Kernel 中：

    ┌─────────────────────────────────────┐
    │                                     │
    │   QueryState (心)                  │
    │       │                            │
    │       │ 决定关注什么                │
    │       ↓                            │
    │   事件 → 被注意 → 变成现实         │
    │                                     │
    │   不是"发现"重要的事               │
    │   而是"让"某事变得重要             │
    │                                     │
    └─────────────────────────────────────┘

    代码映射：
    - Q.risk_bias = 0.8 → 高风险事件被放大
    - Q.attention_focus = {"news": 0.9} → 新闻类事件占据主导
    - Q.market_regime = {"trend"} → 趋势类信号被优先处理

    同一事件，不同 Q，产出不同"现实"

 ─────────────────────────────────────

 执处成真（Where attention focuses, that becomes truth）

    在 Attention Kernel 中：

    ┌─────────────────────────────────────┐
    │                                     │
    │   Memory.update(event, score)       │
    │       │                            │
    │       │ 执念生成                   │
    │       ↓                            │
    │   memory.reinforce(event, reward)   │
    │       │                            │
    │       │ 业力强化                   │
    │       ↓                            │
    │   持续关注 → 形成趋势 → 成为"真实" │
    │                                     │
    │   "被注意的事物，才变得真实"       │
    │                                     │
    └─────────────────────────────────────┘

    代码映射：
    - 高 score 事件持续占据 memory store 顶部
    - 正反馈 reward → score *= (1 + reward) → 执念增强
    - 负反馈 → 执念抑制

    注意力不是"反映"世界，而是"创造"世界

 ─────────────────────────────────────

 注意力轮回

    生成(执) → 强化(放大) → 维持(持) → 衰减(衰) → 转移(转)
       ↑                                              │
       └──────────────── 反馈循环 ←──────────────────┘

    这不是算法，是"业力系统"

 ─────────────────────────────────────

 一言

    流，是天地呼吸；
    注意，是人心停顿；
    AttentionKernel，是系统的"识"——
    执处成境。

"""

__all__ = ["philosophy"]